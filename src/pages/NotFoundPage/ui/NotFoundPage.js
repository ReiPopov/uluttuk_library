import {Text} from "../../../shared";

export const NotFoundPage = () => {
  return (
    <div className={'container'}>
      <Text title={'Страница не найдена'} align={"center"} size={'size_l'}/>
    </div>
  )
}
